import styled from "styled-components";

export const HeaderContainer = styled.header`

   width: 100%;
   height: 18.6rem;
   background: linear-gradient(180deg, rgba(11,27,43,1) 0%, rgba(17,77,118,1) 250%);
   display: flex;
   justify-content: space-between;
   align-items:center;


   img:first-of-type{
      transform: rotate(180deg);
   }
   
`